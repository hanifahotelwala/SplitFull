<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.splitfeatures">

    <uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FeaturesActivity" />
        <activity android:name=".Camera" />
        <activity android:name=".PhotoLibrary" />
        <activity android:name=".workoutsplit.WorkoutSplit" />
        <activity android:name=".Timer" />
        <activity android:name=".workoutsplit.EditData" />
        <activity android:name=".workoutsplit.ListData" />
        <!-- <activity android:name=".workoutsplit.CustomAdapter" /> -->
        <!-- <activity android:name=".workoutsplit.DatabaseHelper" /> -->
        <!-- <activity android:name=".workoutsplit.Parent" /> -->

        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".notes.NoteEditor" />
        <activity android:name=".notes.Notes"></activity>
    </application>

</manifest>